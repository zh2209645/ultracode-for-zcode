---
description: Use a primary-agent-planned dynamic workflow and route atomic tasks to appropriately powered ZCode subagents.
argument-hint: "[task]"
skills: dynamic-workflow
---

Apply the mounted `dynamic-workflow` skill to the following task:

$ARGUMENTS

The primary Agent must retain ownership of decomposition, dependency waves, routing, aggregation, escalation, verification, and the final answer. Do not force delegation when direct work is cheaper and equally reliable.
